<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title }}</title>
  <link href="{{ site.url }}/"/>
  <link rel="self" type="application/atom+xml" href="{{ site.url }}/feed.xml"/>
  <updated>{{ updated }}</updated>
  <id>{{ site.url }}/</id>
  {% if site.author %}<author><name>{{ site.author }}</name></author>{% endif %}
  {% if site.description %}<subtitle>{{ site.description }}</subtitle>{% endif %}
{% for post in posts %}
  <entry>
    <title>{{ post.title }}</title>
    <link href="{{ site.url }}/posts/{{ post.slug }}/"/>
    <id>{{ site.url }}/posts/{{ post.slug }}/</id>
    <updated>{{ post.date }}</updated>
    {% if post.description %}<summary>{{ post.description }}</summary>{% endif %}
    <content type="html">{{ post.content }}</content>
  </entry>
{% endfor %}
</feed>
